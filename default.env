# MLflow settings for local development
# This file provides a default, local-only configuration for MLflow.
# The tracking URI is set to a local directory, and no remote credentials are used.

# Tracking URI for local MLflow (will create an 'mlruns' directory)
MLFLOW_TRACKING_URI=file:./mlruns

# Default experiment name for local runs
MLFLOW_EXPERIMENT_NAME="Poisson Solver Local"

# Set Databricks credentials to empty to ensure they are not used for local execution.
# A user-specific '.env' file can override these for remote tracking.
DATABRICKS_HOST=
DATABRICKS_TOKEN=
