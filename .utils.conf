# Project utilities configuration

[hpc]
# HPC jobs directory (relative to project root)
hpc_dir = "Experiments/HPC-jobs"

# Experiments config filename (within hpc_dir)
experiments_config = "experiments.yaml"

# Runner script (relative to project root)
runner_script = "Experiments/06-scaling/runner.py"

[hpc.lsf]
queue = "hpcintro"
walltime = "00:15"
mem = "4GB"
cores_per_node = 24

# MPI mapping
ranks_per_socket = 12
