# @package _global_
defaults:
  - /config
  - /solver: jacobi
  - _self_

experiment_name: communication

# Halo exchange benchmark
N: 64
n_ranks: 4
max_iter: 200
tolerance: 0.0

# Sweep config for run_solver.py
sweep:
  N: [32, 48, 64, 80]
  strategy: [sliced, cubic]
  communicator: [numpy, custom]
