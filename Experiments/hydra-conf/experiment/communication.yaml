# @package _global_
defaults:
  - /config
  - /solver: jacobi
  - _self_

experiment_name: communication

# Halo exchange benchmark
n_ranks: 4
max_iter: 200
tolerance: 0.0

# Default values (override with -m for sweep)
# uv run python run_solver.py -cn experiment/communication -m N=32,48,64,80 strategy=sliced,cubic communicator=numpy,custom
N: 64
strategy: sliced
communicator: custom
