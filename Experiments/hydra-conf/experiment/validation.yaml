# @package _global_
defaults:
  - /config
  - /solver: jacobi
  - _self_

experiment_name: validation

# Validation settings
N: 32
n_ranks: 8
max_iter: 50000
tolerance: 1.0e-10
strategy: sliced
communicator: custom
